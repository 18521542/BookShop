import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const UserShopScreen = (props) => {
    return (
        <View>
            <Text>User shop screen</Text>
        </View>
    )
}

export default UserShopScreen

const styles = StyleSheet.create({})
